Place the following font files in this directory:

  Astonpoliz.otf   — used for headings / display text
  Avec.otf         — used for body text

Both are referenced in app/globals.css via @font-face.
Until the files are added, the site will fall back to Georgia (headings) and Inter/system-ui (body).
